interface:
  display_name: "Go Mode"
  short_description: "把目标编排成分阶段、可沉淀、可隔离、可并行的执行流"
  default_prompt: "我要进入 go mode：请接住这个目标，先做 Goal Lock、Phase Sketch、Isolation Check、Routing Check；再按阶段 PRD 模板沉淀 phase 文档，判断是否需要 worktree 隔离或并行 agent，然后按 phase gate 执行并验证。"
